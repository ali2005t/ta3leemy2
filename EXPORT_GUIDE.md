# 🚀 دليل تصدير تطبيقات "تعليمي" (Mobile & Windows)

مرحباً يا صديقي! 👋
هذا الملف يشرح لك كيفية استخراج (Build) النسخ النهائية من تطبيقاتك (ملف APK للأندرويد، وملف EXE للكمبيوتر).

المشروع يحتوي على مجلدين رئيسيين، كل واحد منهم يمثل تطبيقاً منفصلاً:

1.  📁 **تطبيق الموبايل (أندرويد)**: موجود في المجلد `mobile_app_template`
2.  📁 **برنامج الكمبيوتر (ويندوز)**: موجود في المجلد `windows_app_template`

---

## 📱 أولاً: تطبيق الموبايل (Android)

للحصول على شرح تفصيلي لتغيير الاسم اللوجو واستخراج ملف APK، افتح الملف التالي:
👉 [دليل تطبيق الموبايل (اضغط هنا)](mobile_app_template/README.md)

**ملخص سريع:**
1.  عدل الإعدادات في `lib/config.dart`.
2.  قم بتغيير اللوجو والاسم.
3.  استخدم الأمر: `flutter build apk --release` (للتجربة).
4.  أو الأمر: `flutter build appbundle --release` (لمتجر جوجل بلاي).

---

## 🖥️ ثانياً: برنامج الكمبيوتر (Windows)

للحصول على شرح تفصيلي لبرنامج الويندوز، افتح الملف التالي:
👉 [دليل برنامج الويندوز (اضغط هنا)](windows_app_template/README.md)

**ملخص سريع:**
1.  عدل الإعدادات في `lib/config.dart`.
2.  قم بتغيير أيقونة البرنامج `.ico`.
3.  استخدم الأمر: `flutter build windows --release`.

---

## 💡 ملاحظات هامة قبل البدء

1.  **تثبيت Flutter**: تأكد أن Flutter مثبت عندك ويعمل بشكل صحيح.
    - جرب كتابة `flutter doctor` في التيرمنال للتأكد.
2.  **تحديث التطبيق**: في المستقبل، إذا قمت بتحديث موقعك الإلكتروني، لن تحتاج لإعادة بناء التطبيق! التطبيقات عبارة عن "متصفح" يفتح موقعك، فأي تحديث على الموقع سيظهر فوراً في التطبيق.
    - **استثناء**: إذا أردت تغيير اللوجو أو اسم التطبيق نفسه، ستحتاج لعمل Build جديد.

## 💡 تلميح: تغيير اسم الملف الناتج (Renaming)

افتراضياً، يقوم Flutter بتسمية الملف `app-release.apk` أو `app-release.aab`.
لا توجد طريقة مباشرة من Flutter لتغيير هذا الاسم أثناء البناء.

**الحل الأبسط:**
قم بإعادة تسمية الملف **يدوياً** بعد انتهاء عملية البناء (كليك يمين -> Rename) وسمه باسم المدرس (مثلاً `Mr_Ahmed.apk`).

**الحل الاحترافي (للسرعة):**
يمكنك استخدام هذا الأمر في التيرمنال ليقوم بالبناء وإعادة التسمية فوراً (غيّر `TeacherName` للاسم الذي تريده):

**للأندرويد (APK):**
```powershell
flutter build apk --release; mv build/app/outputs/flutter-apk/app-release.apk "TeacherName.apk"
```

**للمتجر (AAB):**
```powershell
flutter build appbundle --release; mv build/app/outputs/bundle/release/app-release.aab "TeacherName.aab"
```

---

## 💿 عمل ملف تسطيب (Installer) للكمبيوتر

إذا أردت دمج جميع ملفات البرنامج في ملف واحد `.exe` (مثل `Setup.exe`) لتسهيل التثبيت على الطلاب:

1.  قم بتحميل وتثبيت برنامج **Inno Setup** (مجاني وصغير الحجم).
2.  لقد جهزت لك ملف "سكريبت" جاهز داخل مجلد:
    `windows_app_template/installers/setup_script.iss`
3.  اضغط عليه مرتين ليفتح في Inno Setup.
4.  اضغط على زر **Play** (السهم الأخضر في الأعلى) ▶️.

سيقوم البرنامج بضغط كل الملفات وإنشاء ملف **Sero_Setup_v1.0.exe** في مجلد `installers`.
هذا الملف هو الوحيد الذي ترسله للطلاب! ✅

بالتوفيق! 🚀


