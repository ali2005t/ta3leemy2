# 🖥️ برنامج الكمبيوتر (Windows App)

هذا المشروع عبارة عن نسخة الكمبيوتر من منصتك التعليمية. يعمل كبرنامج مستقل (exe) بدلاً من المتصفح.

## ⚠️ متطلبات هامة جداً قبل البدء

لكي تستطيع بناء برامج الويندوز، يجب أن يكون لديك مايكروسوفت **Visual Studio** (وليس VS Code) مثبتاً مع حزمة **"Desktop development with C++"**.

إذا ظهر لك خطأ `Unable to find suitable Visual Studio toolchain`، فهذا يعني أنك تحتاج لتثبيت هذه الحزمة من مثبت Visual Studio.

## 🛠️ خطوات التجهيز (Configuration)

### 1. تعديل الإعدادات
افتح الملف:
`lib/config.dart`

وعدل البيانات التالية:
- `appName`: اسم البرنامج (يظهر في الشريط العلوي).
- `startUrl`: رابط المنصة.

### 2. تغيير أيقونة البرنامج
للويندوز، نحتاج صورة بصيغة `.ico`.
1. اذهب للمسار: `windows/runner/resources/`
2. استبدل ملف `app_icon.ico` بالأيقونة الخاصة بك.

---

## 🚀 استخراج البرنامج (Build EXE)

1. افتح التيرمنال داخل مجلد `windows_app_template`.
2. نفذ الأوامر التالية:
   ```bash
   flutter clean
   flutter pub get
   ```
3. ابدأ عملية البناء:
   ```bash
   flutter build windows --release
   ```

⏳ **العملية قد تأخذ بضعة دقائق...**

✅ **أين أجد البرنامج؟**
بعد الانتهاء، ستجد المجلد الكامل للبرنامج في:
`build/windows/runner/Release/`

⚠️ **هام جداً:**
عند إرسال البرنامج للطلاب، **يجب إرسال مجلد `Release` كاملاً** (يمكنك ضغطه في ملف ZIP)، وليس ملف `.exe` فقط، لأن الملف يحتاج للملفات المجاورة له ليعمل.
داخل المجلد، سيقوم الطالب بفتح الملف الذي ينتهي بـ `.exe`.
