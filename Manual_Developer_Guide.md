# الدليل اليدوي لتطوير تطبيقات Ta3leemy (Manual Developer Guide) 🛠️🚀

هذا الدليل يشرح لك كيفية تعديل وبناء التطبيقات يدوياً بدون أي أدوات خارجية.

---

## 1. أين أعدل الرابط واسم التطبيق؟ (Configuration) ⚙️
لتغيير الرابط (Link) والاسم (Name) واللون (Color)، قم بتعديل الملف التالي:
*   **المسار:** `lib/config.dart` (موجود داخل مجلد `mobile_app_template` ومجلد `windows_app_template`).

**كود الملف:**
```dart
class AppConfig {
  static const String appName = "Ta3leemy Teacher"; // ✏️ غيّر الاسم هنا
  static const String appUrl = "https://example.com"; // ✏️ غيّر رابط موقعك هنا
  static const Color primaryColor = Color(0xFF2ecc71); // ✏️ غيّر اللون الرئيسي
}
```

---

## 2. كيف أغير أيقونة التطبيق؟ (App Icon) 🎨
لتغيير اللوجو الذي يظهر على الموبايل أو سطح المكتب:

1.  جهز صورة اللوجو بصيغة **PNG** (يفضل حجم 1024x1024).
2.  سمّها `logo.png`.
3.  استبدل الصورة القديمة الموجودة في المسار:
    *   `assets/images/logo.png`
4.  افتح التيرمنال (Terminal) داخل مجلد المشروع (موبايل أو ويندوز) واكتب الأمر:
    ```bash
    flutter pub run flutter_launcher_icons
    ```
    *هذا الأمر سيقوم بإنشاء جميع الأيقونات تلقائياً لكل المقاسات.*

---

## 3. كيف أغير رقم الإصدار؟ (Version) 🔢
لتحديث التطبيق (مثلاً من 1.0.0 إلى 1.0.1):

1.  افتح ملف `pubspec.yaml` (موجود في المجلد الرئيسي).
2.  ابحث عن السطر:
    ```yaml
    version: 1.0.0+1
    ```
3.  غيّره إلى الجديد، مثلاً:
    ```yaml
    version: 1.0.1+2
    ```
    *(الرقم بعد علامة + هو رقم البناء Build Number ويجب أن يزيد كل مرة).*

---

## 4. كيف أبني التطبيق؟ (Build Commands) 🏗️

### 🅰️ للأندرويد (Mobile)
افتح التيرمنال داخل مجلد `mobile_app_template` واكتب:

*   **للمتجر (Google Play - AAB):**
    ```bash
    flutter clean
    flutter build appbundle --release
    ```
    📍 *مكان الملف:* `build/app/outputs/bundle/release/app-release.aab`

*   **للتجربة المباشرة (APK):**
    ```bash
    flutter build apk --release
    ```
    📍 *مكان الملف:* `build/app/outputs/flutter-apk/app-release.apk`

---

### 🅱️ للويندوز (Windows Desktop)
افتح التيرمنال داخل مجلد `windows_app_template` واكتب:

*   **لبناء البرنامج (EXE):**
    *(ملاحظة: كلمة bash مجرد رمز للكود، لا تكتبها، اكتب الأوامر التي تحتها فقط)*
    ```bash
    flutter clean
    flutter build windows --release
    ```
    📍 *مكان الملف:* `build/windows/x64/runner/Release/`
    *(ستجد ملف `sera.exe` والملفات المساعدة بجانبه. لكي يعمل البرنامج، يجب نقل المجلد كاملاً وليس الملف فقط).*

*   **لعمل ملف تثبيت (Installer Setup - exe واحد):**
    1.  يوجد ملف جاهز اسمه `setup.iss` داخل مجلد `windows_app_template`.
    2.  حمل برنامج **Inno Setup** وثبته.
    3.  افتح ملف `setup.iss` (كليك يمين -> Open with Inno Setup).
    4.  اضغط زر التشغيل (Play ▶️) في البرنامج.
    5.  سيظهر لك ملف `AliYousef_Setup_v1.0.exe` بجوار الملف.

---

## 5. كيف أنقل المشروع لجهاز آخر؟ 💻📦
فقط انسخ المجلدين:
1.  `mobile_app_template`
2.  `windows_app_template`

وعند تشغيلهم على الجهاز الجديد لأول مرة، اكتب الأمر:
```bash
flutter pub get
```
ليقوم بتحميل المكاتب اللازمة.

---
**بالتوفيق في مشروعك! ❤️**
