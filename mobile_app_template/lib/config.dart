import 'dart:ui';

/// ============================================================
/// ⚙️ ملف الإعدادات (Configuration)
/// هذا هو الملف الوحيد الذي تحتاج لتعديله لإنشاء تطبيق لمدرس جديد.
/// ============================================================

class AppConfig {
  
  // 1. 📛 اسم التطبيق (بيظهر في بعض التنبيهات)
  static const String appName = "hossamsaid";

  // 2. 🔗 رابط المنصة الخاص بالمدرس
  // ضع الرابط الذي يفتح عليه التطبيق أول ما يشتغل
  static const String startUrl = "https://smmviip.com";

  // 3. 🎨 اللون الأساسي للتطبيق
  // (يستخدم في شاشة التحميل والشرائط العلوية)
  // 0xFF + كود اللون السداسي (مثلاً 0xFF6366f1)
  static const Color primaryColor = Color(0xFF6366f1);

  // 4. 🕵️‍♂️ معرف المتصفح (User Agent)
  // بيساعد الموقع يعرف إن ده "تطبيق موبايل" مش متصفح عادي
  static const String userAgent = "Ta3leemyApp/1.0 (Mobile)";

  // 5. 🖼️ مسار اللوجو (داخل مجلد assets/images)
  // تأكد إنك حطيت الصورة في الفولدر ده بنفس الاسم
  static const String logoAssetPath = "assets/images/logo.png";

  // 6. 🛑 إعدادات الحماية
  // هل تريد منع أخذ سكرين شوت؟ (تعمل فقط على أندرويد هنا، الأيفون يحتاج إعدادات أخرى)
  static const bool preventScreenshots = true;
}
