# 📱 تطبيق المدرس (Mobile App)

هذا المشروع عبارة عن تطبيق Flutter يقوم بعرض منصتك التعليمية داخل تطبيق موبايل احترافي (WebView).

## 🛠️ خطوات التجهيز (Configuration)

قبل استخراج التطبيق، تأكد من تعديل البيانات الخاصة بك. كل التعديلات تتم في ملف واحد فقط!

### 1. تعديل بيانات التطبيق
افتح الملف التالي:
`lib/config.dart`

ستجد بداخله متغيرات واضحة للتعديل:
- `appName`: اسم التطبيق الذي سيظهر تحت الايقونة.
- `startUrl`: رابط موقعك (تأكد أنه يبدأ بـ https).
- `primaryColor`: اللون الأساسي للتطبيق.

### 2. تغيير اللوجو (الأيقونة)
1. قم بوضع صورة اللوجو الجديد (يفضل تكون مربعة وبجودة عالية PNG) داخل المجلد:
   `assets/images/`
   واسمها يجب أن يكون `logo.png` (استبدل الصورة القديمة).

2. في التيرمنال (Terminal) وأنت داخل مجلد `mobile_app_template`، نفذ الأمر التالي لتحديث أيقونة التطبيق في الأندرويد:
   ```bash
   flutter pub run flutter_launcher_icons
   ```

### 3. تغيير "اسم الحزمة" (Package Name)
اسم الحزمة هو المعرف الوحيد للتطبيق على المتجر (مثل `com.ta3leemy.teacher`).
لتغييره بسهولة:
1. ابحث في كل الملفات (CRTL + SHIFT + F) عن `com.example` أو الاسم القديم.
2. استبدله بالاسم الخاص بك (مثلاً `com.yoursite.app`).
3. تأكد من تعديل الملف `android/app/build.gradle`.

---

## 🚀 استخراج ملف APK (Build)

بعد الانتهاء من التعديلات، اتبع الخطوات التالية لاستخراج الملف النهائي:

1. افتح التيرمنال داخل مجلد `mobile_app_template`.
2. نظف المشروع للتأكد من عدم وجود ملفات قديمة:
   ```bash
   flutter clean
   flutter pub get
   ```
3. قم ببناء ملف APK (للتجربة والنشر المباشر):
   ```bash
   flutter build apk --release
   ```

4. **للنشر على متجر Google Play (مهم جداً!)**:
   المتجر لا يقبل APK، بل يطلب ملف بصيغة `.aab`. لاستخراجه استخدم الأمر:
   ```bash
   flutter build appbundle --release
   ```

   ℹ️ **ملاحظة هامة**: لرفع التطبيق على المتجر، ستحتاج لإنشاء "مفتاح توقيع" (Keystore). هذا موضوع تقني قليلاً، لكن الأمر أعلاه سينتج لك الملف المبدئي.

⏳ **انتظر قليلاً...** سيقوم الكمبيوتر بتحميل المكتبات وبناء التطبيق.

✅ **أين أجد الملفات؟**
- ملف **APK**: `build/app/outputs/flutter-apk/app-release.apk`
- ملف **AAB** (للمتجر): `build/app/outputs/bundle/release/app-release.aab`

يمكنك إرسال ملف APK للمدرسين مباشرة، أما AAB فهو مخصص للرفع على Google Play Console.

⚠️ **تجاهل هذا الخطأ إن ظهر لك:**
قد تظهر لك رسالة باللون الأحمر في النهاية تقول:
`Release app bundle failed to strip debug symbols from native libraries`
هذا **ليس خطأ حقيقياً**! هو مجرد تنبيه بأن حجم الملف أكبر قليلاً من المعتاد، ولكنه **سليم 100% وجاهز للرفع على المتجر** ولا يحتاج لأي إصلاح.

