# 📋 قائمة إصدار تطبيق جديد (New Client Checklist)

استخدم هذه القائمة في كل مرة يأتيك عميل جديد (مدرس جديد) لضمان عدم نسيان أي خطوة.

## 1️⃣ التجهيز (مرة واحدة فقط لكل عميل)
*   [ ] **نسخ المشروع:** قم بعمل نسخة جديدة من مجلد `mobile_app_template` ومجلد `windows_app_template` وضعهم في مجلد جديد باسم العميل.
*   [ ] **تجهيز اللوجو:** احفظ لوجو العميل بصيغة `png` وسمّه `logo.png` (يفضل أبعاد 1024x1024).

---

## 2️⃣ إعدادات الأندرويد (Mobile) 📱

1.  **استبدال اللوجو:**
    *   انسخ `logo.png` وضعه في: `mobile_app_template/assets/images/` (وافق على الاستبدال).

2.  **تحديث البيانات:**
    *   افتح ملف: `mobile_app_template/lib/config.dart`
    *   عدل `appName` (اسم المدرس).
    *   عدل `startUrl` (رابط المنصة).
    *   عدل `primaryColor` (لون التطبيق).

3.  **توليد الأيقونات:**
    *   افتح التيرمنال في `mobile_app_template`.
    *   اكتب: `flutter pub run flutter_launcher_icons`

4.  **تحديث الإصدار (اختياري):**
    *   من ملف `pubspec.yaml` عدل `version: 1.0.0+1` (زود الرقم الأخير).

5.  **البناء (Build):**
    *   نظف المشروع: `flutter clean`
    *   ابنِ ملف المتجر: `flutter build appbundle --release`
    *   ابنِ ملف التجربة: `flutter build apk --release`

✅ **الملفات الناتجة:** ستجدها داخل `mobile_app_template/build/app/outputs/`.

---

## 3️⃣ إعدادات الويندوز (PC) 💻

1.  **استبدال اللوجو:**
    *   انسخ `logo.png` وضعه في: `windows_app_template/assets/images/` (وافق على الاستبدال).

2.  **تحديث البيانات:**
    *   افتح ملف: `windows_app_template/lib/config.dart`
    *   عدل `appName` وزود الرابط واللون مثل الأندرويد.

3.  **توليد الأيقونات:**
    *   افتح التيرمنال في `windows_app_template`.
    *   اكتب: `flutter pub run flutter_launcher_icons` or `dart run flutter_launcher_icons`

4.  **تحديث ملف التثبيت (Setup Script):**
    *   افتح ملف `setup.iss` الموجود في مجلد الويندوز (أو `installers/setup_script.iss` حسب ما تفضل).
    *   عدل `AppName` و `AppPublisher` باسم المدرس.
    *   عدل `AppPublisherURL` برابط المنصة.
    *   عدل `OutputBaseFilename` ليكون مثلاً `TeacherName_Setup_v1`.

5.  **البناء (Build):**
    *   نظف المشروع: `flutter clean`
    *   ابنِ ملف التنفيذ: `flutter build windows --release`

6.  **إنشاء ملف التثبيت (Installer):**
    *   شغل ملف `setup.iss` ببرنامج **Inno Setup**.
    *   اضغط **Run**.

✅ **الملف الناتج:** سيظهر ملف `.exe` بجوار ملف `setup.iss` يحمل اسم المدرس، وهذا هو الملف الذي ترسله للعميل.
